---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
## Configuration and akmods for ublue-main kernels

modules:
  - type: kargs
    arch: x86_64, aarch64
    kargs:
      - preempt=full

  - type: akmods
    base: main
    install:
      - v4l2loopback
